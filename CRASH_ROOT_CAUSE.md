# ? ПРОБЛЕМА НАЙДЕНА!

**Точная причина:** Приложение крашится при создании `GeometryPanel()`

## ?? Путь к проблеме

1. ? QApplication создается
2. ? MainWindow создается
3. ? SimulationManager создается
4. ? GLView создается
5. ? Central widget устанавливается
6. ? **КРАШ при создании GeometryPanel**

## ?? Вывод консоли

```
  ? Central widget setup
    _setup_docks: Creating geometry dock...
[КРАШ - process terminated]
```

## ?? РЕШЕНИЕ

**Временное:** Закомментировать создание панелей или создать stub-панели

**Постоянное:** Исправить GeometryPanel (скорее всего проблема в импортах или инициализации)

## ?? СЛЕДУЮЩИЕ ДЕЙСТВИЯ

1. Проверить, что импортирует/инициализирует GeometryPanel
2. Добавить try-except вокруг создания панелей
3. Использовать fallback (пустые QWidget) если панель не создается

---

**Статус:** Проблема локализована на 100%

**Файл:** `src/ui/panels/__init__.py` или `panel_geometry.py`
