#!/usr/bin/env bash
# Automatically activate the PneumoStabSim environment when entering the repo.
REPO_ROOT=$(pwd)
if [ -f "$REPO_ROOT/activate_environment.sh" ]; then
  # shellcheck disable=SC1090
  source "$REPO_ROOT/activate_environment.sh"
fi
