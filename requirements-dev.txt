# Aggregated development dependencies pinned with uv.
#
# Чтобы обновить файл requirements-dev.lock выполните:
#   uv pip compile pyproject.toml --extra dev -o requirements-dev.lock --generate-hashes
#
# Устанавливает базовые и дев-зависимости из единого локфайла.
-r requirements-dev.lock
