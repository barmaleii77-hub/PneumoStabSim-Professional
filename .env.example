# PneumoStabSim - пример .env для локальной настройки окружения
# Переименуйте в .env и адаптируйте под свою машину

# Принудительный бэкенд рендера (Windows: d3d11, Linux/macOS: opengl)
QSG_RHI_BACKEND=d3d11

# Логирование Qt (умолчание: отключено)
QT_LOGGING_RULES=*.debug=false;*.info=false

# Информация рендера (0/1)
QSG_INFO=0

# Масштабирование HiDPI
QT_ENABLE_HIGHDPI_SCALING=1
QT_AUTO_SCREEN_SCALE_FACTOR=1
QT_SCALE_FACTOR_ROUNDING_POLICY=PassThrough

# Диагностика приложения (1 = печатать сводки по логам после выхода)
PSS_DIAG=1

# Обход проверки версии Python (используйте только при необходимости)
# PSS_IGNORE_PYTHON_CHECK=1

# Путь до локальных QML модулей/плагинов, если Qt установлен нестандартно
# QML2_IMPORT_PATH=C:\Qt\6.10.2\qml
# QT_QML_IMPORT_PATH=C:\Qt\6.10.2\qml
# QT_PLUGIN_PATH=C:\Qt\6.10.2\plugins

# Выбор HDR по умолчанию (относительно assets/qml)
# START_IBL_SOURCE=../hdr/studio_small_09.hdr
# START_IBL_FALLBACK=../hdr/studio_small_09.hdr
# START_IBL_INTENSITY=1.0
# START_SKYBOX_ENABLED=1
# START_IBL_ENABLED=1
