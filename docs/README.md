# PneumoStabSim Professional v2.0.1 — Documentation Hub

Актуальное краткое руководство по навигации в документации и инструментам проекта.

## 🚀 Quick Start

1. **Подготовьте окружение**
   - Linux/macOS: `uv sync --frozen --extra dev`
   - Windows без `make`: `python -m tools.task_runner uv-sync`
   - Если `uv` не установлен, запустите `python scripts/bootstrap_uv.py`.
2. **Запустите приложение**
   - `uv run python app.py` (или `python app.py` при активированной `.venv`)
3. **Пройдите контроль качества**
   - Linux/macOS: `make check`
   - Windows: `python -m tools.task_runner check`
   - Автономный отчёт с логами: `python -m tools.task_runner autonomous-check`
4. **Линтеры и типы по отдельности**
   - `python -m tools.ci_tasks lint`
   - `python -m tools.ci_tasks typecheck`
   - `python -m tools.ci_tasks qml-lint`
   - `python -m tools.ci_tasks test`

## 🌈 HDR ассеты, динамический диапазон и сохранение текстур

### Новые возможности

- **Единый манифест + автозагрузка** — `assets/hdr/hdr_manifest.json` теперь
  используется как источник правды для валидации и скачивания: команда
  `uv run python -m tools.task_runner verify-hdr-assets --fetch-missing` пополняет
  `.cache/hdr_assets/` и сравнивает SHA-256, исключая устаревшие или дублированные
  панорамы.
- **Подсветка и телеметрия путей** — `MainWindow.normalizeHdrPath()`
  конвертирует относительные и абсолютные пути в `file://` URL, а
  `FileCyclerWidget` сразу показывает предупреждение и тултип, если HDR/EXR не
  найден. Все попытки резолвинга логируются для отладки кроссплатформенных путей.
- **Устойчивые пользовательские текстуры** — `FileCyclerWidget` на панелях
  Graphics/Materials хранит выбранные пути (`ibl_source`, `texture_path`) в
  `config/app_settings.json` и восстанавливает их после перезапуска, даже если
  ассеты временно отсутствуют.

### Практика использования

- **Нормализация и fallback** — логика обнаружения описана в
  `src/ui/panels/graphics/environment_tab.py` и покрыта тестами
  (`tests/unit/ui/test_hdr_discovery.py`, `tests/unit/ui/test_main_window_hdr_paths.py`).
  Поддерживаются относительные пути, `file://` URL и удалённые ссылки; отсутствующие
  файлы помечаются красным индикатором.
- **Динамический диапазон** — вкладка Effects экспонирует слайдеры `HDR Maximum`
  (0.0–10.0, шаг 0.1) и `HDR Scale` (1.0–5.0, шаг 0.1). Значения сохраняются в
  `current.graphics.effects.bloom_hdr_max` и `current.graphics.effects.bloom_hdr_scale`
  через `GraphicsSettingsService`, затем применяются QML-окружением в
  `assets/qml/effects/SceneEnvironmentController.qml`.
- **Текстуры материалов** — селекторы текстур используют тот же `FileCyclerWidget`
  и проверяют наличие файлов в `assets/qml/assets/` или по сохранённым путям.
  При отсутствии ассета включается предупреждение, но последнее валидное значение
  сохраняется, чтобы пользовательские наборы не терялись.
- **Рекомендации по QA** — фиксируйте выбранный HDR, `bloom_hdr_max` и
  `bloom_hdr_scale` в отчётах регрессионных скриншотов; это позволяет воспроизвести
  визуальную сцену после обновления ассетов или переезда проекта.

**Быстрый чек-лист синхронизации HDR**

```bash
uv run python -m tools.task_runner verify-hdr-assets --fetch-missing --force-rehash
cp .cache/hdr_assets/studio_small_09_2k.hdr assets/hdr/
uv run python app.py --safe --ibl ../hdr/studio_small_09_2k.hdr
```

## 🧯 Safe/Legacy Launch Profiles
- **`python app.py --safe` / `--test-mode`** — единый headless-сценарий для smoke
  тестов: Qt не создаёт сцену, `QCoreApplication` завершается после проверки
  окружения. Используется во всех CI jobах.
- **`python app.py --legacy`** — фиксирует OpenGL-бэкенд Qt Quick и отключает
  RHI. Совместим с `--safe` для воспроизводимых Mesa-прогонов.
- **Headless окружения** — экспортируйте `QT_QPA_PLATFORM=offscreen`,
  `QT_QUICK_BACKEND=software`, `QSG_RHI_BACKEND=opengl`, `LIBGL_ALWAYS_SOFTWARE=1`.
  На macOS допускается `QT_QPA_PLATFORM=minimal` + `QT_MAC_WANTS_LAYER=1` для
  Metal без окон. Полный рецепт описан в `docs/CI.md`.

## 🔄 Pneumatic Update Guarantees
- Интегратор (`src/physics/integrator.py`) вычисляет углы рычагов с клипованием
  и передаёт их в `PneumaticSystem.update_system_from_lever_angles`.
- `PneumaticSystem` (модуль `src/pneumo/system.py`) проверяет диагональные
  соединения, собирает объёмы линий, валидирует положительность и выдаёт
  структурированный отчёт.
- Газовая сеть (`src/pneumo/network.py`) получает обновлённые объёмы,
  пересчитывает давления, применяет изоляцию master valve и логирует расчёты.
- Любая несогласованность (отсутствующие цилиндры, отрицательный объём,
  некорректные ключи) приводит to исключению, поэтому шаг считается завершённым
  только после успешного обновления всех подсистем.

## 📁 Project Layout (v2.0.1)
```
PneumoStabSim-Professional/
├── app.py                         # Точка входа (uv run python app.py)
├── src/
│   ├── app_runner.py              # Bootstrap + интеграция UI/логики
│   ├── common/                    # Логирование, настройки, bridge
│   └── ui/
│       ├── main_window_pkg/       # Главное окно и маршрутизатор сигналов
│       └── panels/
│           ├── graphics/          # Модульная панель графики (tabs + state)
│           ├── geometry/          # Геометрия и параметры подвески
│           ├── lighting/          # Панель освещения
│           └── modes/             # Исторический модуль режимов
├── assets/
│   └── qml/                       # Qt Quick 3D сцены и панели
├── config/                        # JSON-настройки и схемы валидации
├── tools/
│   ├── task_runner.py             # Python-обёртка для целей make
│   ├── autonomous_check.py        # Сборка quality-gate отчётов
│   ├── ci_tasks.py                # Линтеры, типы, pytest, qmllint
│   └── environment/               # Проверка Qt/GL окружения
├── tests/                         # PyTest suites (unit, integration, ui, manual)
│   └── manual/                    # Ручные сценарии диагностики (не входят в CI)
├── uv.lock                        # Зафиксированные версии зависимостей
└── reports/                       # История тестов, качества, производительности
```

## 🧩 Ключевые компоненты
- **GraphicsPanel (`src/ui/panels/graphics/panel_graphics.py`)** — координатор вкладок после завершённого рефакторинга фаз 3.x, собирает состояние через `collect_state()` и публикует сигналы.
- **GraphicsSettingsService (`src/ui/panels/graphics/panel_graphics_settings_manager.py`)** — слой доступа к JSON-настройкам и дефолтам.
- **Defaults & presets (`src/ui/panels/graphics/defaults.py`)** — генерация пресетов качества и освещения.
- **QML панели (`assets/qml/panels/`)** — визуальные элементы, синхронизированные с Python через SignalsRouter.

## 📄 Где искать подробности
| Документ | Описание |
|----------|----------|
| [`docs/RENOVATION_MASTER_PLAN.md`](RENOVATION_MASTER_PLAN.md) | Стратегия модернизации и текущая фаза |
| [`docs/RENOVATION_PHASE_3_UI_AND_GRAPHICS_PLAN.md`](RENOVATION_PHASE_3_UI_AND_GRAPHICS_PLAN.md) | Дорожная карта для UI/графики |
| [`docs/SETTINGS_ARCHITECTURE.md`](SETTINGS_ARCHITECTURE.md) | Архитектура настроек и схемы |
| [`docs/graphics.md`](graphics.md) | Технические заметки по графике и эффектам |
| [`reports/`](../reports/) | Автоматические отчёты тестов, качества, производительности |

## 🧪 Testing & QA Checklist
- `python -m tools.ci_tasks verify` — lint + typecheck + pytest + qmllint + анализ артефактов.
- `python -m tools.task_runner autonomous-check` — аналог Makefile-цели с отчётом и launch-trace.
- `python -m tools.project_sanitize` — очистка временных файлов (используется перед автономным прогоном).
- `python -m tools.environment.verify_qt_setup --report-dir reports/environment` — проверка системных библиотек Qt/GL.
- Ручные сценарии (например, `tests/manual/centralized_save_demo.py`) выполняются точечно и не входят в CI.

## 🔧 Инструменты и советы
- Настройки QML lint берутся из `qmllint_targets.txt`. Если файл пуст или отсутствует, `tools.ci_tasks` завершится ошибкой — заполните путь к папкам/файлам QML.
- Для Windows-пользователей без `make` все цели доступны через `python -m tools.task_runner <task>`.
- `scripts/bootstrap_uv.py` автоматически скачивает и устанавливает `uv`.
- В `.env` задаются `QT_QPA_PLATFORM`, `QT_QUICK_CONTROLS_STYLE` и другие переменные среды для Qt.

## ❓ Troubleshooting
- **PySide6 ImportError:** запустите `python -m tools.environment.verify_qt_setup --allow-missing-runtime`.
- **Проблемы с qmllint:** проверьте `qmllint_targets.txt` и наличие Qt SDK (`pyside6-qmllint`).
- **Headless Qt:** убедитесь, что заданы `QT_QPA_PLATFORM=offscreen` и `QT_QUICK_BACKEND=software`, на macOS используйте `minimal` + `QT_MAC_WANTS_LAYER=1`.
- **Windows и PowerShell:** активируйте окружение через `activate_environment.ps1`, после чего используйте `python -m tools.task_runner ...`.
- **Нет uv:** установите через `python scripts/bootstrap_uv.py` либо используйте системный Python и `pip install -r requirements-dev.txt`.

> Документация поддерживается в актуальном состоянии для релиса v2.0.1. Обновляйте её при изменении архитектуры, tooling или путей к ключевым документам.

## Примечание релиза v2.0.1

Актуализация версий и синхронизация с metadata.version.
