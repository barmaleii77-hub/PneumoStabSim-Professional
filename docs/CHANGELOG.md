# 📦 Changelog

## [Unreleased]
### Added
- TBD

### Changed
- TBD

## [4.9.8] - 2027-06-07
### Added
- Обновлены `docs/README.md` и `assets/hdr/README.md` с новыми возможностями HDR
  пайплайна: подробные инструкции по журналированию резолвинга путей
  (`logs/ibl/ibl_events.jsonl`), расширенные примеры аудитов и быстрые сценарии
  проверки после миграции проекта. Добавлена «Telemetry quick view» с разбором
  полей `platform` и `status`, а также рекомендации по включению логов в
  автономные отчёты.
- Добавлены релизные заметки HDR-ветки `hdr/bloom-tonemap` (v4.9.8) c ссылками
  на обязательные проверки `hdr-verify`, сохранение нормализованных путей и
  требования к манифестам для Windows и Linux.

### Changed
- Удалены устаревшие упоминания о «запланированном» HDR, рекомендации приведены в
  соответствие с production-потоком и автотестами, покрывающими `file://` URL и
  дубликаты каталогов. Tooltip и отчёты теперь объясняют, как устранить
  проблемы с манифестом или путями, чтобы убрать статус `⚠ файл не найден`.
- Версионные ссылки синхронизированы с `pyproject.toml` (`4.9.8`), чтобы
  сопроводительные документы, HDR-гайды и релизные заметки сообщали одну и ту
  же целевую сборку.

## [4.9.5] - 2027-05-07
### Added
- Консолидирована HDR-ветка `hdr/bloom-tonemap`: отдельные ползунки `HDR Maximum` и
  `HDR Scale` дополняют существующий порог и сохраняются через сервис настроек,
  закрывая все требования по тонмаппингу и пост-обработке.
- Панель окружения использует `FileCyclerWidget` для перебора HDR skybox-файлов,
  позволяет очищать выбор и выводит предупреждение/подсказку, если ресурс
  отсутствует в манифесте или на диске.
- Добавлены релизные заметки HDR-ветки (`docs/release_notes/hdr_branch.md`) с
  быстрым обзором изменений, требованиями к манифестам и шагами миграции
  конфигураций.

### Changed
- Главное окно нормализует HDR-пути, приводя локальные URL и логируя каждую
  неудачную попытку резолвинга, чтобы упростить кроссплатформенную отладку.
- Обновлена HDR-документация (`docs/README.md`, `assets/hdr/README.md`) с
  примерами загрузки, советами по кешированию и ссылками на манифестный
  пайплайн.
- Уточнены инструкции по HDR-ассетам, динамическому диапазону и сохранению
  текстур: текущий пайплайн отмечен как работающий в продакшене, добавлены
  диапазоны для `HDR Minimum` и правила восстановления пользовательских
  текстур из конфигурации.
- В хабе документации уточнена поддержка текстур, чтобы устранить устаревшие
  сообщения «не поддерживается» и согласовать инструкции с текущим UX
  `FileCyclerWidget`.

### Notes
- Функционал HDR доставлен в релиз из ветки `hdr/bloom-tonemap`: в сборке
  сохранена единая схема тонмаппинга, FileCyclerWidget и журналирование путей,
  описанные в валидационных чек-листах хаба документации и `assets/hdr/README.md`.
