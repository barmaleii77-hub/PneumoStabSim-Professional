# ENVIRONMENT_SETUP.md — Qt и Python окружение

Документ объединяет сведения, которые раньше были разбросаны по `docs/run.md`,
`.github/copilot-instructions.md`, `docs/TROUBLESHOOTING.md` и отчётам об
окружении. Используйте его как единую точку правды при настройке рабочих и
headless-профилей.

## 1. Поддерживаемые профили Python/Qt

| Профиль | Назначение | Python | PySide6/QtQuick3D | NumPy | SciPy | Примечания |
| --- | --- | --- | --- | --- | --- | --- |
| **Основной** | Ежедневная разработка, CI | 3.13.x | 6.10.0 | 2.0.1 | 1.13.1 | Используется `make uv-sync` и `python tools/setup_qt.py --qt-version 6.10.0`. |
| **LTS** | Поддержка прежних инсталляций | 3.11.x | 6.5.3 | 1.24.4 | 1.11.4 | Требует extras-профиля `pneumostabsim[qt65]`. |
| **Legacy** | Ручная проверка миграций | 3.12.x | 6.8.1 | 1.26.4 | 1.12.0 | Не входит в стандартный `uv`-профиль, создаётся вручную. |

> 💡 Дополнительные комбинации (PySide6 6.6/6.7/6.9) поддерживаются в режиме
> best-effort. Устанавливайте их через extras (`.[qt66]`, `.[qt69]`) и фиксируйте
> результат в `reports/quality/environment_setup_report.md`.

## 2. Базовые переменные окружения Qt

Эти значения автоматически проставляются `src/bootstrap/environment.py` и
прописываются в `env.sample`. При ручном запуске убедитесь, что они загружены:

| Переменная | Значение | Назначение |
| --- | --- | --- |
| `QSG_RHI_BACKEND` | `opengl` | Единый backend для Windows, Linux и macOS по умолчанию. |
| `QT_QUICK_CONTROLS_STYLE` | `Fusion` | Согласованный стиль контролов везде. |
| `QSG_OPENGL_VERSION` | `3.3` | Минимальная версия OpenGL. |
| `QT_OPENGL` | `desktop` | Принудительное использование desktop OpenGL. |
| `QSG_INFO` | `0` | Отсечка шумных логов сцены. |
| `QT_LOGGING_RULES` | `*.debug=false;*.info=false` | Глобальное снижение уровня логирования Qt. |
| `QT_AUTO_SCREEN_SCALE_FACTOR` | `1` | Корректное масштабирование HiDPI. |
| `QT_SCALE_FACTOR_ROUNDING_POLICY` | `PassThrough` | Стабильное масштабирование UI. |
| `QT_ENABLE_HIGHDPI_SCALING` | `1` | Включение HiDPI. |
| `QT_ASSUME_STDERR_HAS_CONSOLE` | `1` | Корректная маршрутизация stderr. |
| `PSS_DIAG` | `1` | Включение диагностического канала симулятора. |

## 3. Headless и Vulkan сценарии

| Сценарий | Обязательные переменные | Дополнительные шаги |
| --- | --- | --- |
| **Linux headless (CI, контейнер)** | `QT_QPA_PLATFORM=offscreen`, `QT_QUICK_BACKEND=software`, `LIBGL_ALWAYS_SOFTWARE=1` | Убедитесь, что `DISPLAY` не установлен. Команда `make uv-run CMD="pytest -q"` автоматически применит профиль. Запускайте `./setup_linux.sh` для установки `libGL`, `libxkbcommon`, `qt6-shader-baker` и Python-зависимостей PySide6/QtQuick3D перед прогоном тестов. |
| **Vulkan smoke** | `QSG_RHI_BACKEND=vulkan`, `VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/lvp_icd.x86_64.json` | Путь к ICD проверьте `ls /usr/share/vulkan/icd.d/`. Для headless добавьте `QT_QPA_PLATFORM=offscreen`. |
| **Windows ANGLE fallback** | `QT_QPA_PLATFORM=windows`, `QT_OPENGL=angle` | Используйте при проблемах с драйверами OpenGL. Возвращаясь к GPU, удалите переменные или задайте `QT_OPENGL=desktop`. |
| **Safe mode (software-only)** | `LIBGL_ALWAYS_SOFTWARE=1`, `QT_QUICK_BACKEND=software` | В Python можно вызвать `configure_qt_environment(safe_mode=True)`; переменные подхватятся автоматически. |

## 4. Проверка окружения

1. `make uv-sync` или `python -m tools.task_runner uv-sync` — синхронизация
   зависимостей.
2. `python tools/setup_qt.py --qt-version 6.10.0` — установка Qt Quick 3D.
3. `make check` — агрегированная проверка (`ruff`, `mypy`, `pytest`, `qmllint`).
4. `python -m tools.environment.verify_qt_setup --expected-version 6.10` —
   убедиться, что PySide6 найден и соответствует целевой версии.
5. Сохраните вывод в `reports/quality/environment_setup_report.md` при обновлении
   профилей.

### Кроссплатформенное тестирование

- Linux/macOS:

  ```sh
  make cross-platform-prep
  make cross-platform-test
  ```

- Windows / среды без `make`:

  ```powershell
  python -m tools.task_runner cross-platform-prep -- --qt-version 6.10.0
  python -m tools.task_runner cross-platform-test -- --pytest-args tests
  ```

Скрипт `tools/cross_platform_test_prep.py` проверяет наличие системных пакетов,
импорт PySide6/`pytest-qt` и автоматически включает headless-настройки Qt
(`QT_QPA_PLATFORM=offscreen`, `QT_QUICK_BACKEND=software` для Linux,
`QSG_RHI_BACKEND=d3d11` для Windows) при сохранении стиля `QT_QUICK_CONTROLS_STYLE=Fusion`
для полного соответствия рантайму приложения. На Linux устанавливается полный
набор библиотек, идентичный Dockerfile (`git`, `curl`, `pkg-config`,
`build-essential`, Mesa/Qt стэк `libegl-dev`, `libgles-dev`, `libx11-*`,
`libxcb-*`, `mesa-utils-extra`, `libvulkan1`, `mesa-vulkan-drivers`, `vulkan-tools` и др.),
что исключает пропуски даже для QML/3D сценариев. Выполняйте эти команды после
**каждого** изменения кода, чтобы сохранить паритет тестов между платформами.
Проверка импортов происходит через интерпретатор `.venv`, сформированный `uv`,
поэтому запуск скрипта из системного Python не приводит к ложным «отсутствует
pytest-qt» — проверяется именно тот набор пакетов, который будет использоваться
в тестах.
Начиная с версии 4.9.8 тестовый раннер больше не пропускает GUI/QML-тесты при
отсутствии зависимостей — он завершает прогон с ошибкой и предлагает повторно
запустить `python -m tools.cross_platform_test_prep --use-uv`, поэтому отсутствие
Qt или `pytest-qt` немедленно выявляется. Если Python-окружение формируется
вручную, обязательно вызовите `./setup_linux.sh` либо `powershell -File scripts/setup_windows.ps1`
для установки модулей PySide6 (`QtWidgets`, `QtQuick3D`, `QtQml`) и пакета Shader
Baker.

## 5. HDR окружение и тонемаппинг

- Выполните `python -m tools.render_checks.validate_hdr_orientation` перед сборкой
  релизных билдаов. Скрипт сверяет `config/baseline/materials.json` и отчёт
  `reports/performance/hdr_orientation.md`, проверяя ориентацию skybox-ов,
  допустимость статусов и совпадение файлов; любое расхождение помечает сборку
  как ошибочную, поэтому обновляйте отчёт сразу после добавления новой HDR-карты.
- При добавлении HDR/EXR-файлов запустите модульные проверки:
  `python -m pytest tests/unit/ui/test_hdr_discovery.py` и
  `python -m pytest tests/unit/ui/test_main_window_hdr_paths.py`. Первый тест
  гарантирует, что `GraphicsPanel` сканирует `assets/hdr/`, `assets/hdri/` и
  `assets/qml/assets/`, второй — что `normalizeHdrPath` строит корректный
  список кандидатов и выдаёт предупреждения при отсутствующих файлах.
- При тестировании пресетов тонемаппинга синхронизируйте значения через панель
  `TonemappingPanel`: пресеты управляют `tonemap_mode`, экспозицией и белой
  точкой и применяются сразу к `SettingsManager`, поэтому снимаются снапшоты
  HDR-параметров без расхождений с runtime.

## 6. Скрипты подготовки окружения

- `./setup_linux.sh` — настраивает headless Linux-среду: устанавливает системные
  пакеты (`libGL`, `libxkbcommon`, Xvfb, Mesa, Shader Baker), синхронизирует
  Python-зависимости через `uv` (либо `pip`) и экспортирует headless-переменные
  `QT_QPA_PLATFORM=offscreen`, `QT_QUICK_BACKEND=software`, `QSG_RHI_BACKEND=opengl`.
- `powershell -File scripts/setup_windows.ps1` — выполняет `uv sync` (с
  возможностью пропустить через `-SkipUvSync`), гарантирует наличие колёс PySide6,
  `PyOpenGL`, экспортирует headless-настройки (`QT_QPA_PLATFORM=offscreen`,
  `QT_OPENGL=software`, `QSG_RHI_BACKEND=d3d11`).

Оба скрипта могут использоваться как локально, так и в CI. Они предназначены
для устранения пропусков тестов, связанных с отсутствием библиотек Qt/OpenGL.

> ⛔ Любой пропущенный тест теперь завершает сессию `pytest` со статусом ошибки.
> Для временного обхода используйте `--allow-skips` или `PSS_ALLOW_SKIPPED_TESTS=1`,
> но сразу после восстановления зависимостей возвращайтесь к строгому режиму.

## 5. Дополнительные рекомендации

- При запуске из VS Code или Copilot GPT сначала выполните `make cipilot-env`,
  затем загрузите переменные из `.env.cipilot` (`source ./.env.cipilot`).
- Для headless тестов `pytest` используйте флаг `--pss-headless` или экспортируйте
  `PSS_HEADLESS=1`; он подхватит значения из таблицы выше.
- При смене backend (OpenGL ↔ Vulkan) очищайте кэш Qt (`rm -rf ~/.cache/QtProject`)
  и перезапускайте приложение, чтобы исключить артефакты прошлой сессии.
- Всегда обновляйте этот документ при добавлении новых профилей или переменных;
  остальные инструкции должны ссылаться именно на него.
