# Additional pytest targets can be listed here if a temporary suite needs to run.
# The default CI and local tasks always execute the full tests/ tree first; this
# file only appends extra paths and must never be used to exclude required
# suites.
